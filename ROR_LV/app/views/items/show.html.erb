<div class="col-md-6 col-md-offset-3">
	<h2><%= @item.title %></h2>
	<p><%= @item.description %></p>

	<%= link_to "Editar", edit_item_path(@item) %>
	<%= link_to "Borrar", item_path(@item), method: :delete, data: { confirm: "Estas seguro?" } %>
	<%= link_to "Volver", root_path %>
</div>